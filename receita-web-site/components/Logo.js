import Router from 'next/router';

const Logo = () => {
    return (
        <img src="img/logo/logo.jpeg" width={30} height={30} className="rounded mr-1 d-inline-block align-top" alt="logo" loading="lazy" />
    )
}

export default Logo;